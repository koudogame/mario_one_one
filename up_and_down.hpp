#pragma once

// 体の上下
struct UpDown
{
    int right_head_x;   // 右頭
    int right_head_y;   // 右頭

    int right_foot_x;   // 右足
    int right_foot_y;   // 右足
                           
    int left_head_x;    // 左頭
    int left_head_y;    // 左頭
                           
    int left_foot_x;    // 左足
    int left_foot_y;    // 左足
};